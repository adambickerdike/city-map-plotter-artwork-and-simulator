# Exact Python geometry environment used for the reviewed SVG cohort.
# CPython 3.13.9
# GEOS 3.13.1 (bundled by the reviewed Shapely wheel)
#
# Create the environment with Python 3.13.9, then run:
#   python -m pip install -r requirements/university-v2.1.3.txt
#   python -m pip install --no-deps -e .
#
# PNG previews additionally used Inkscape 1.4.4. SVG geometry does not depend
# on Inkscape or system fonts; the single-stroke display font is bundled.
numpy==2.5.1
shapely==2.1.2
